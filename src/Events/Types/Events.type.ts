enum Events {
  PartlyRecognized = 'partly-recognized',
  FullyRecognized = 'recognized',
  VoicesReady = 'voices-ready',
  PermissionStatusReady = 'permissions-ready',
  Ready = 'ready',
}

export default Events;
